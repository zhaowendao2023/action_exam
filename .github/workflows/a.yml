name: GLaDOS Auto Checkin

on:
  workflow_dispatch:
  schedule:
    - cron: '55 1 * * *'

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: pwd
      - run: ls
  job2:
    runs-on: ubuntu-latest
    steps:
      - run: node --version
